#include <tuple>
#include <regex>
#include <iostream>
#include <string>

int f(int x, int y) { return x * x + y * y + 10; }
int g(int x, int y, int z) { return x * y * z + x * y + x * z + y * z; }
int r(int z) { return z * z * z * z - 10 * z * z; }

int main() {


	return 0;
}